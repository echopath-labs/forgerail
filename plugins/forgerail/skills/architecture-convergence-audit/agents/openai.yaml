interface:
  display_name: "Architecture Convergence Audit"
  short_description: "Assess engineering structure, drift and duplicate ownership."
  brand_color: "#B45309"
  default_prompt: "Use $architecture-convergence-audit to assess this engineering structure against its accepted choices and recommend the smallest useful change without editing the workspace."
policy:
  allow_implicit_invocation: true
