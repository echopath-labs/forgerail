# No-Node documentation fixture

This fixture intentionally has no `package.json`, `node_modules`, or `.forgerail/` directory. It models a documentation-only project that uses Agent Plugin Skills without a project-local JavaScript runtime.
