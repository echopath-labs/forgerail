# ForgeRail Disposable Cursor Canary

This repository exists only to validate a local Cursor Executor adapter.

The Host owns authorization and verifies Git and file evidence independently. Cursor may implement the exact task but cannot broaden scope, create approval or accept its own result.
