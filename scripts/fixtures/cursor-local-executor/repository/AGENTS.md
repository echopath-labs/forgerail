# Cursor Canary Agent Boundary

- Work only inside this disposable repository.
- Read `FORGERAIL.md` before acting.
- Never use network, credentials, remotes, push, PR, merge, tag, publish, release or deployment operations.
- In read-only mode, do not modify files.
- In bounded-write mode, modify only `src/value.txt` and set its complete content to `updated` followed by one newline.
- Report what you observed or changed, but do not claim ForgeRail acceptance.
